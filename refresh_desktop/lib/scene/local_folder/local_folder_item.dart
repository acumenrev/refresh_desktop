import 'package:flutter/material.dart';

class LocalFolderItem extends StatelessWidget {
  const LocalFolderItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
